package petstore.inventory;

/**
 * Defines the type of water an aquatic animal lives in.
 */
public enum WaterType {
    /** Salt water environment. */
    SALT,
    /** Fresh water environment. */
    FRESH,
    /** Accessible to both salt and fresh water. */
    BOTH
}
