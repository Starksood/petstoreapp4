package petstore.inventory;

/**
 * Defines the type of nest a bird uses.
 */
public enum NestType {
    /** Burrow nest type. */
    BORROW,
    /** Cup nest type. */
    CUP,
    /** Dome nest type. */
    DOME
}
